name: currency_converter
description: A Flutter application for converting currencies.
publish_to: 'none' # Remove this line if you want to publish your package

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.18.1
  # Alte dependențe pot fi adăugate aici

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Moldova.png
    - assets/USA.png
